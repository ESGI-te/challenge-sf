{% extends 'base.html.twig' %}

{% block title %}Edit Ingredient{% endblock %}

{% block main %}
    <div class="container d-flex justify-content-between">
        <div class="row"></div>
        {{ include('partials/_adminnav.html.twig') }}
        <main class="col-9 border border-light bg-white p-2">
            <h1>Edit Ingredient</h1>

            {{ include('ingredient/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="btn btn-secondary"  href="{{ path('app_ingredient_index') }}">back to list</a>

            {{ include('ingredient/_delete_form.html.twig') }}
        </main>
    </div>

{% endblock %}
