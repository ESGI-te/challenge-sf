{% extends 'base.html.twig' %}

{% block title %}RecipeMood{% endblock %}

{% block body %}
    <h1>RecipeMood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_mood.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recipe_mood.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_mood_index') }}">back to list</a>

    <a href="{{ path('app_recipe_mood_edit', {'id': recipe_mood.id}) }}">edit</a>

    {{ include('recipe_mood/_delete_form.html.twig') }}
{% endblock %}
