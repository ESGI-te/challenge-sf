{% extends 'base.html.twig' %}

{% block title %}RecipeCourse{% endblock %}

{% block body %}
    <h1>RecipeCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_course.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recipe_course.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_course_index') }}">back to list</a>

    <a href="{{ path('app_recipe_course_edit', {'id': recipe_course.id}) }}">edit</a>

    {{ include('recipe_course/_delete_form.html.twig') }}
{% endblock %}
