{% extends 'base.html.twig' %}

{% block title %}RecipeMealtime{% endblock %}

{% block body %}
    <h1>RecipeMealtime</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_mealtime.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recipe_mealtime.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_mealtime_index') }}">back to list</a>

    <a href="{{ path('app_recipe_mealtime_edit', {'id': recipe_mealtime.id}) }}">edit</a>

    {{ include('recipe_mealtime/_delete_form.html.twig') }}
{% endblock %}
