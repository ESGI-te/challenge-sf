{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>


        {% for recipe in recipes %}
            {{ recipe.content }}
        {% else %}
            <tr>
                <td colspan="11">no recepes found</td>
            </tr>
        {% endfor %}
    <br>
    <a href="{{ path('user_index') }}">Go back to list</a>
{% endblock %}
