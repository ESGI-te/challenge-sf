<form method="post" action="{{ path('app_recipe_difficulty_delete', {'id': recipe_difficulty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe_difficulty.id) }}">
    <button class="btn btn-danger mt-1">Supprimer</button>
</form>
