{% extends 'base.html.twig' %}

{% block title %}RecipeDuration{% endblock %}
{% block main %}

    <div class="container d-flex justify-content-between">
        <div class="row"></div>
        {{ include('partials/_adminnav.html.twig') }}
        <main class="col-9 border border-light bg-white p-2">
            <h1>RecipeDuration</h1>

            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ recipe_duration.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ recipe_duration.name }}</td>
                </tr>
                </tbody>
            </table>

            <a class="btn btn-secondary" href="{{ path('app_recipe_duration_index') }}">back to list</a>

            <a class="btn btn-success" href="{{ path('app_recipe_duration_edit', {'id': recipe_duration.id}) }}">edit</a>

            {{ include('recipe_duration/_delete_form.html.twig') }}
        </main>
    </div>

{% endblock %}
