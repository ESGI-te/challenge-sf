{% extends 'base.html.twig' %}

{% block title %}RecipeDuration{% endblock %}

{% block body %}
    <h1>RecipeDuration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_duration.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recipe_duration.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_duration_index') }}">back to list</a>

    <a href="{{ path('app_recipe_duration_edit', {'id': recipe_duration.id}) }}">edit</a>

    {{ include('recipe_duration/_delete_form.html.twig') }}
{% endblock %}
