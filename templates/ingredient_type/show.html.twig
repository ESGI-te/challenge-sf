{% extends 'base.html.twig' %}

{% block title %}IngredientType{% endblock %}

{% block main %}
    <div class="container d-flex justify-content-between">
        <div class="row"></div>
        {{ include('partials/_adminnav.html.twig') }}
        <main class="col-9 border border-light bg-white p-2">
            <h1>IngredientType</h1>

            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ ingredient_type.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ ingredient_type.name }}</td>
                </tr>
                </tbody>
            </table>

            <a class="btn btn-secondary"  href="{{ path('app_ingredient_type_index') }}">back to list</a>

            <a  class="btn btn-success"  href="{{ path('app_ingredient_type_edit', {'id': ingredient_type.id}) }}">edit</a>

            {{ include('ingredient_type/_delete_form.html.twig') }}
        </main>
    </div>

{% endblock %}
