<form method="post" action="{{ path('app_recipe_mood_delete', {'id': recipe_mood.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe_mood.id) }}">
    <button class="btn">Delete</button>
</form>
